/*
Что выведет код ниже?

alert( 1 && null && 2 ); //используем И, он ищет первую НЕ истину, так что выведем null

 */

/*
Что выведет код ниже?

alert( alert(1) && alert(2) ); //тут опять используем и, алерт возвращает андефайнед,, слева направо выполняется так - выводим 1, получаем фолс,тк андейфайнед и в итоге андефайнед
 */